{
  "compilerOptions": {
    // JavaScript 출력 버전 (ES2018 = ES9)
    "target": "es2018",
    // 모듈 시스템 (CommonJS - Node.js 호환)
    "module": "commonjs",
    // 사용할 라이브러리 타입 정의
    "lib": ["es2018"],
    // JSX 변환 방식 (React 17+ 새로운 JSX Transform)
    "jsx": "react-jsx",
    // 엄격한 타입 체크 활성화
    "strict": true,
    // 모듈 해결의 기준 디렉토리
    "baseUrl": ".",
    // 소스 파일의 루트 디렉토리
    "rootDir": "src",
    // 컴파일된 파일 출력 디렉토리
    "outDir": "dist",
    // 모듈 해결 전략 (Node.js 방식)
    "moduleResolution": "node",
    // default export가 없는 모듈도 default import 허용
    "allowSyntheticDefaultImports": true,
    // ES 모듈과 CommonJS 간 상호 운용성
    "esModuleInterop": true,
    // 라이브러리 타입 체크 건너뛰기 (빌드 속도 향상)
    "skipLibCheck": true,
    // .d.ts 타입 선언 파일 생성
    "declaration": true,
    // .d.ts.map 소스맵 파일 생성
    "declarationMap": true,
    // .js.map 소스맵 파일 생성
    "sourceMap": true,
    // 파일 출력 활성화 (false면 타입 체크만)
    "noEmit": false,
    // 경로 별칭 매핑 (babel alias와 동일하게 설정)
    "paths": {
      "@/*": ["./src/*"],
      "@app/*": ["./src/app/*"],
      "@shared/*": ["./src/shared/*"]
    },
    // 포함할 타입 정의 패키지
    "types": ["react-native", "node"]
  },
  // 컴파일할 파일 패턴
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  // 제외할 디렉토리/파일
  "exclude": ["dist", "node_modules"]
}
